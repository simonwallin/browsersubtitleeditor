var express = require('express');
var router = express.Router();
//const db = require("./../db/db");

/* GET home page. */
/*router.get('/', function(req, res, next) {
  db.db.collection("users").find().then((users, err) => {
    res.render('admin', { users: users });
  })
});*/

module.exports = router;
